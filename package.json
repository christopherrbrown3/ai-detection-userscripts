{
  "name": "ai-detection-userscripts",
  "version": "0.3.0",
  "private": true,
  "description": "Build and test tooling for local AI-style signal userscripts.",
  "scripts": {
    "build": "python3 scripts/build_userscripts.py",
    "build:check": "python3 scripts/build_userscripts.py --check",
    "test": "node --test tests/*.test.mjs"
  },
  "devDependencies": {
    "jsdom": "^26.1.0"
  },
  "engines": {
    "node": ">=20"
  }
}
